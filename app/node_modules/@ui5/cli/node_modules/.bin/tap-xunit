#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../tap-xunit/bin/tap-xunit" "$@"
  ret=$?
else 
  node  "$basedir/../tap-xunit/bin/tap-xunit" "$@"
  ret=$?
fi
exit $ret
