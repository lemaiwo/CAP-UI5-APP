const ServiceClient = require ('./ServiceClient')

module.exports = class DatabaseService extends ServiceClient {
    // eslint-disable-next-line no-unused-vars
    async deploy (model, options) { throw new Error ('cds.deploy should be implemented in subclass') }
}
