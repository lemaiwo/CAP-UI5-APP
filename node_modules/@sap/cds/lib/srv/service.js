const cds = require ('@sap/cds-reflect')
require = cds.lazified(module)  // eslint-disable-line

/**
 * The `cds.builtin.classes.service` decorated with accessors to
 * service bindings, providers, runtime classes, etc.
 */
module.exports = cds.extend (cds.builtin.classes.service) .with ({
    bindings: require ('./bindings'),
    classes: require ('./classes'),
    providers: [],
    impl: x=>x,
})
